<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">記事管理システム</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= articles_path %>">記事一覧</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= discard_index_articles_path %>">削除記事</a>
        </li>
      </ul>
      <div class="d-flex">
        <% if current_user %>
          <div class="d-flex m-1"><a class="btn btn-outline-success" href="<%= new_article_path %>">記事登録</a></div>
          <div class="d-flex m-1"><%= link_to "ログアウト", destroy_user_session_path, method: :get, class: "btn btn-outline-danger" %></div>
        <% end %>
        <% if !current_user %>
          <div class="d-flex"><a class="btn btn-outline-primary" href="<%= new_user_session_path %>">ログイン</a></div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
